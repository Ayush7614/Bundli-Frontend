import { createTheme } from "@mui/material/styles";

export const customTheme = createTheme({

  palette: {

    primary: {
      
      main: "#ff1493",
      black: "#000000",
      white: "#FFFFFF",
    },
  },
});
